#include<stdio.h>
#include<string.h>
int main(){
    //%s inbuilt function that directly prints a string without loop.
char str[]="hello Nandani";

printf("%s\n",str);
puts(str);
//puts prints the string.
    return 0;
}