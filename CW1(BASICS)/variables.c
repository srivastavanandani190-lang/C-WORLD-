#include<stdio.h>
int  main(){

    /*C language is case sensitive language!
  variable stores value ,datatype refers to type of data variable is storing.
   int,float,char(basic datatype); array,pointer,enums(derived datatype)
   KEYWORDS (32 IN C LANGUAGE) */

   int a=31;
   float b=9.9;
   char c='N';
   printf("The value of a is %d\n",a);
   printf("The value of b is %f\n",b);
   printf("The value of c is %c\n",c);
   return 0;
}