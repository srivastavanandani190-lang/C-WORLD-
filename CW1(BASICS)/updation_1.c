#include<stdio.h>
int main(){
    int x=5;
    printf("The value of x is %d\n",x);
    x=x+10;
    printf("Now the value of x is %d\n",x);
    x=x-3;
    printf("The value of x is now %d\n",x);
    x=x*5;
    printf("the value of x is %d\n",x);
    return 0;
}
//variable should start with alphabet or underscore(only special charater can used) not with number!
//no other special characters are allowed.
//case sensistive.
//keywords are mainly small alphabet format.
/*
    int a; //variable declaration
    a=32; //variable initialisation
    printf("The value of a is %d",a);
    return 0; //successful termination
*/
