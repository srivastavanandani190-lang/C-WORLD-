//int size=10,b[size];->correct.
//num[4]->fifth elements of array.
//memory allocation in array is continous (integer->4bytes)
//if semicolon after for loop(condition) it means loops end there.
//garbage value(if no value is present then byitself it assign garbage value)
//inside any loop it's important to check either i is assigned any datatype or not.
//int num[5]->array holding five elements ; num[5]=11->fifth element with 4 index have value as 11.
//index of any arrays range from (0 to n-1).(if more than this then index out of bound error).
//num[27]->28th element of array.

#include<stdio.h>
int main(){
int arr[4];
printf("%d ",arr[0]);

    return 0;
}